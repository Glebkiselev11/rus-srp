version: '3'
services:
  server:
    build: 
      context: .
    environment:
      DATABASE_URL: database.db
      SALT: 123456
    ports:
      - "8080:8080"
    volumes:
      - ./database.db:/database.db
      - ./migrations:/migrations
      - ./src:/server/src
