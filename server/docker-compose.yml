version: '3'
services:
  server:
    build: 
      context: .
    ports:
      - "8080:8080"
    volumes:
      - ./database.db:/database.db
      - ./migrations:/migrations
      - ./src:/server/src
