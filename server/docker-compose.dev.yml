services:
  server:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    environment:
      - DATABASE_URL=database.db
      - SALT=123456
    ports:
      - "8080:8080"
    volumes:
      - ./database.db:/database.db
      - ./migrations:/migrations
      - ./src:/server/src